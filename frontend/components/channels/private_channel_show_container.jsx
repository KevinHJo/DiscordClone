import { connect } from "react-redux";
import PrivateChannelShow from './channel_show';

const mSTP = (state, { match }) => ({
    
});

const mDTP = dispatch => ({

});

export default connect(mSTP, mDTP)(PrivateChannelShow)